<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cooldudes.stalkmarket">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">



        <activity android:name="cooldudes.stalkmarket.ui.activity.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.Onboarding1Activity" />
        <activity android:name=".ui.activity.Onboarding2Activity" />
        <activity android:name=".ui.activity.Onboarding3Activity" />
        <activity android:name=".ui.activity.Onboarding4Activity" />
        <activity android:name=".ui.activity.DiversifyActivity" />
        <activity android:name=".ui.activity.SupplyActivity" />
        <activity android:name=".ui.activity.CompoundActivity" />

        <activity android:name=".ui.activity.MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <receiver
            android:name=".helper.AlarmReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
    </application>

</manifest>